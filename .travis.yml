# deploy:
#   provider: pages
#   github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
#   keep_history: true
#   fqdn: zachzhao.dev
#   on:
#     branch: master

language: ruby
dist: bionic
before_deploy: 
  - gem update --system
deploy:
  - provider: pages
    github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
    keep_history: true
    fqdn: zachzhao.dev
    on:
      branch: master
